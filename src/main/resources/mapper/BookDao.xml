<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 映射 接口 BookDao -->
<mapper namespace="com.zxj.demo.dao.BookDao">

    <!-- 这里的id 是 接口中的方法名  -->
    <select id="bookDao" resultType="com.zxj.demo.entity.Book">
        select * from book
    </select>

</mapper>